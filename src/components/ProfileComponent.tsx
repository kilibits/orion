import * as React from "react";
import { Profile } from "../common/models/Profile";

const ProfileApp = (_profile: Profile) => {

    //retrieve profile photo using profileId
    return(
        <div>
            <h2>Display Base Profile Here ...</h2>
        </div>
    )
}

export default ProfileApp;