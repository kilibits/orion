import * as React from 'react';

interface IMapProps {
  location?: boolean,
  streetView?: boolean
}

const MapComponent: React.FunctionComponent<IMapProps> = (props: IMapProps) => {
  return(
    <>
    </>
  )
}

export default MapComponent;